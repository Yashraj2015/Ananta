function isWriteCommand(buf) {
  return false; // Implement proper parsing
}

function rewriteResponseTo(buf, newId) {
  return buf; // Implement proper parsing
}

module.exports = { isWriteCommand, rewriteResponseTo };